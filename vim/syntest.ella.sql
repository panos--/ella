
var baz := fun () {
	{
	}
};
if ({=> true; }.eval()) {
	sql {
		select * from foo;
		{
			select * from foo;
		}
	}
}
fun foo(foo, bar, bla, blubb) {
    select foo from bar;
    sql {
        select foo from bar;
    }
    select foo from bar;
    {
        select foo from bar;
        select foo from bar;
    }
    select foo from bar;
}
select * from foo;
var foo := 'foo';

for foo() {
    sql {
    }
}

var foo := 'foo';
fun foo(laskdlakd) {
}

var foo = fun (foo) {
    {
        select foo from dual;
    }
    select foo from
};

sql select * from foo;

select * from foo;

{
    sql select * from foo;
    'fooo';
    sql {
        select * from foo;
    }
}
